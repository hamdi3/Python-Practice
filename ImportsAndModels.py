#moudle , Pakete und importe
#dealing with moudle
import math #doing this we got the whole math lib
print(math.ceil(2.4))
from math import ceil #doing this we just imported ceil 
print(ceil(2.7)) #since we only imported ceil no need to refrence math
print(dir(math)) # to know all funcs in math
print(help(ceil)) # to know exactly what a func does
